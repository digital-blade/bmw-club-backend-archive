spring:
  application:
    name: "example-service"

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: "http://localhost:9000/eureka"

basic:
  service:
    name: "Basic Service Implementation"
    icon: 127791