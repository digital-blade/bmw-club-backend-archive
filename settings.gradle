rootProject.name = 'bmw-club-backend'

def microservices = [
        'eureka-server',
        'gateway-service',
        'example-service',
]

microservices.each {
    include(it)
    project(':' + it).projectDir = file('services/' + it)
}
