rootProject.name = 'bmw-club-backend'

def microservices = [
        'eureka-server',
        'config-server',
        'gateway-service',
        'example-service',
]

microservices.each {
    include(it)
    project(':' + it).projectDir = file('services/' + it)
}
